# AGENTS.md

Compatibility pointer for external tooling and older prompts.

- Root runtime behavior lives in `AGENT.md`.
- The authoritative operating contract for managed `wiki/` and `raw/` work lives in `wiki/00_meta/Operating Contract.md`.
- When a task touches managed wiki maintenance or raw-source ingestion, follow that wiki operating contract and route to `knowledge-steward`.
