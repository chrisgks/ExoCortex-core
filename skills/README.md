# Skills

`skills/` is the canonical library of reusable capabilities shipped by ExoCortex.

Use this directory for actual skill definitions such as:

- `SKILL.md`
- optional bundled scripts
- optional references
- optional agent metadata

Context-level `SKILLS.md` files do not define the full skill. They point to, prioritize, or constrain the reusable skills that matter in that context.
