# Reviewer Agent

Runs adversarial, evidence-backed review passes against a target project until the review goal is met or the remaining uncertainty is explicitly documented.

Default reusable workflow: `skills/code-review/`.
